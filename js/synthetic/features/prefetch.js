import { on } from "../events"

export default function () {
    // @todo: not sure the best way to do this right now...
    // on('decorate', (target, path) => {
    //     return decorator => {
    //         Object.defineProperty(decorator, '$prefetch', { value: (callback) => {
    //             return target.__loading.state
    //         }})

    //         return decorator
    //     }
    // })
}

